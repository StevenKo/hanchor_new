= link_to "Add New Tag", new_admin_news_tag_path, class: "btn btn-s btn-blue inlineblock-imp"

%table.admin-table
  %tr
    %th 編號
    %th 標籤名
    %th 標籤名(英文)

  - @tags.each do |tag|
    %tr
      %td= tag.id
      %td= tag.name
      %td= tag.name_en
      %td= link_to 'Edit', edit_admin_news_tag_path(tag)
      %td= link_to 'Destroy', admin_news_tag_path(tag), method: :delete, data: { confirm: 'Are you sure?' }
