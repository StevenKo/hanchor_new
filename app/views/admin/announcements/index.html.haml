= link_to "Add New 訊息", new_admin_announcement_path, class: "btn btn-s btn-blue inlineblock-imp"

%table.admin-table
  %tr
    %th 編號
    %th 訊息內容
    %th 連結
    %th 排序

  - @announcements.each do |announcement|
    %tr
      %td= announcement.id
      %td= announcement.message
      %td= announcement.link
      %td= announcement.sort
      %td= link_to 'Edit', edit_admin_announcement_path(announcement)
      %td= link_to 'Destroy', admin_announcement_path(announcement), method: :delete, data: { confirm: 'Are you sure?' }

%nav.page-menu.relative
  = will_paginate(@announcements, :renderer => 'BootstrapPaginationHelper::LinkRenderer')